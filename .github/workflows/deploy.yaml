name: MSA Build & Push

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  DOCKERHUB_USERNAME: wo9697

jobs:

# -------------------------------------------------
# AUTH SERVICE
# -------------------------------------------------
  build-auth:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Docker Hub Login
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push auth-service
        uses: docker/build-push-action@v5
        with:
          context: ./auth-service
          push: true
          tags: |
            wo9697/auth-service:v1


# -------------------------------------------------
# INCIDENT SERVICE
# -------------------------------------------------
  build-incident:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Docker Hub Login
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push incident-service
        uses: docker/build-push-action@v5
        with:
          context: ./incident-service
          push: true
          tags: |
            wo9697/incident-service:v1


# -------------------------------------------------
# BOARD SERVICE
# -------------------------------------------------
  build-board:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Docker Hub Login
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push board-service
        uses: docker/build-push-action@v5
        with:
          context: ./board-service
          push: true
          tags: |
            wo9697/board-service:v1


# -------------------------------------------------
# FRONTEND SERVICE
# -------------------------------------------------
  build-frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Docker Hub Login
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push frontend-service
        uses: docker/build-push-action@v5
        with:
          context: ./frontend-service
          push: true
          tags: |
            wo9697/frontend-service:v1


# -------------------------------------------------
# API GATEWAY
# -------------------------------------------------
  build-gateway:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Docker Hub Login
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push api-gateway
        uses: docker/build-push-action@v5
        with:
          context: ./api-gateway
          push: true
          tags: |
            wo9697/api-gateway:v1
