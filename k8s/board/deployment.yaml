apiVersion: apps/v1
kind: Deployment
metadata:
  name: board-service
  namespace: incident
spec:
  replicas: 2
  selector:
    matchLabels:
      app: board-service
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: board-service
    spec:
      containers:
      - name: board
        image: wo9697/board-service:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 8003
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "300m"
            memory: "256Mi"
