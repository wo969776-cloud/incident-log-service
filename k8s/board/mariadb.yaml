apiVersion: v1
kind: Service
metadata:
  name: mariadb-board
  namespace: incident
spec:
  clusterIP: None
  selector:
    app: mariadb-board
  ports:
  - port: 3306
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mariadb-board
  namespace: incident
spec:
  serviceName: mariadb-board
  replicas: 1
  selector:
    matchLabels:
      app: mariadb-board
  template:
    metadata:
      labels:
        app: mariadb-board
    spec:
      nodeSelector:
        kubernetes.io/hostname: node3.labs.local
      tolerations:
      - key: "db"
        operator: "Equal"
        value: "only"
        effect: "NoSchedule"
      containers:
      - name: mariadb
        image: mariadb:10.6
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: password
        - name: MYSQL_DATABASE
          value: board_db
        volumeMounts:
        - name: mariadb-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mariadb-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: longhorn
      resources:
        requests:
          storage: 5Gi
